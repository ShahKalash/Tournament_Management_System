<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="style/form-t.css">
    <title>Tournament Registration</title>
</head>

<body>
    <div class="container">
        <h1>City</h1>
        <!-- <form class="mb-5" action="/add_tournament" method="post">
            <div class="form-group">
                <label for="tournament-name">Check availablity fo:</label>
                <input type="text" id="tournament-name" name="t-name" placeholder="Example: Indian Premier League" required>
            </div>
            
            <button type="submit">Register</button>
            <button class="back-btn" onclick="window.location.href = '/';">Back</button>
        </form> -->
        <form action="/add_city" method="post">
            <div class="form-group">
                <label for="city-name">City Name:</label>
                <input type="text" id="city-name" name="city-name" placeholder="Example: Ahmedabad" required>
            </div>
            
            <div class="form-group">
                <label for="state">State:</label>
                <select id="state" name="state" required>
                    <option value="">Select Type</option>
                        <% state_temp.forEach(function(state, i){%>
                        <option value="<%= state.state_id %>"><%= state.state_name %></option>
                        <%}); %>
                </select>
            </div>
            
            <button type="submit">Register</button>
            <button class="back-btn" onclick="window.location.href = '/';">Back</button>
        </form>
    </div>
</body>

</html>